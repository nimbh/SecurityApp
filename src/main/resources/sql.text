create table person(
    id int primary key generated by default as identity,
    username varchar(100) NOT NULL,
    birth int NOT NULL,
    password varchar NOT NULL
);

insert into person(username, birth, "password") VALUES ('user_1', 2001, 'qwerty'),
                                                     ('user_2', 1993, 'qwerty');